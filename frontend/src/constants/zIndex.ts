/** Stacking order for layered overlays (low → high). */
export const Z_INDEX = {
  jobActionModal: 240,
  confirmDialog: 250,
  duplicatesModalBackdrop: 110,
  duplicatesModal: 111,
  duplicateContextMenu: 200,
} as const;
